scp -i "mk-key.pem" export_firebase_data.py build_embeddings.py build_faiss.py marketlink-app.json ubuntu@ec2-34-227-56-36.compute-1.amazonaws.com:/home/ubuntu/product-search/


 //to login
ssh -i "mk-key.pem" ubuntu@ec2-13-218-245-133.compute-1.amazonaws.com

ssh -i "custom-feed.pem" ubuntu@ec2-13-217-97-212.compute-1.amazonaws.com


 df -h //show storage usage

cd ~/product-search // to go to project directory

ls -lh  //show files inside project directory

nano filename.exe //create/edit file

rm filename.py //delete file

cat filename.py //show file content

 source venv/bin/activate //activate virtual environment

tail -f nohup.out //check logs

kill -9 $(lsof -t -i:8000)
//kill

cat file.exe //display

when changing code in the server

ps aux | grep uvicorn
kill -9 <PID>


nohup uvicorn app:app --host 0.0.0.0 --port 8000 & 

//restart server
sudo systemctl restart product-search
//show last 50 logs
journalctl -u product-search.service --no-pager -n 50
//show logs in real time
journalctl -u product-search.service -f

head -n 20 filename.txt

tail -n 20 filename.txt

free -h
